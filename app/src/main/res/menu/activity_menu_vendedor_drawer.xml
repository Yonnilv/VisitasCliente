<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/visitareg"
            android:title="Registro de Visita" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ubicacion"
            android:title="Reportar Ubicacion" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="@string/menu_slideshow"
            android:visible="false"
            />
        <item
            android:id="@+id/nav_tools"
            android:icon="@drawable/ic_menu_manage"
            android:title="@string/menu_tools"
            android:visible="false"
            />
    </group>

    <item android:title="Reportes">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/repvisitas"
                android:title="visitas Realizadas" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/menu_send"
                android:visible="false"
                />
        </menu>
    </item>

</menu>
